import MapResourcesCell from 'src/components/MapResource/MapResourcesCell'

const MapResourcesPage = ({ map_id = 2, item_id = 5 }) => {
  return <MapResourcesCell map_id={map_id} item_id={item_id} />
}

export default MapResourcesPage
