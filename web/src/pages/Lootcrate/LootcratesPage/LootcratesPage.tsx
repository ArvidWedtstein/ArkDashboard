import LootcratesCell from 'src/components/Lootcrate/LootcratesCell'

const LootcratesPage = ({ map, search, types, color }) => {
  return <LootcratesCell map={map} search={search} types={types} color={color} />
}

export default LootcratesPage
